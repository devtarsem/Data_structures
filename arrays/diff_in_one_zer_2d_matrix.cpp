#include<iostream>
#include<vector>
using namespace std;

int main(){
    vector<int> grid = {
        {0,1,1},
        {1,0,1},
        {0,0,1},
    };

    vector<vector<int>> diff(grid.size(), vector<int>(grid[0].size(), 0));

    for(int i)

}